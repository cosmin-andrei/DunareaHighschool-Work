#include <iostream>
#include <cstring>
using namespace std;

int main()
{
    char s[101],*p;

    p=strtok(s," ")
    while(p)
    {
        for(int)
        if(s[i]==s[i+1])
        {
            strcpy()
        }
    }

    return 0;
}
